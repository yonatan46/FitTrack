# Supabase setup

Run `schema.sql` in the Supabase SQL editor to create the first Phase 1 tables and Row Level Security policies. Add the project URL and publishable key to `.env.local` before using the browser or server clients in `src/lib/supabase`.
