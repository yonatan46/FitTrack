# Supabase project settings → API
NEXT_PUBLIC_SUPABASE_URL=

# Use ONE of the following (the "publishable" / "anon" key — never the secret key):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
